import React, { useState, useRef } from 'react';

const Countdown = () => {
    const [days, setDays] = useState('00');

    let interval = useRef();
    
    const startTimer = () => {
        const countdown
    }
}